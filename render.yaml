services:
  - type: web
    name: pdpl-chatbot-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd backend && python run.py
    envVars:
      - key: GROQ_API_KEY
        sync: false
    autoDeploy: true
